/* Copyright (c) 2025 Huawei Technologies Co., Ltd.
 * This file is a part of the CANN Open Software.
 * Licensed under CANN Open Software License Agreement Version 1.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 * ===================================================================================================================*/

/*********************************************************************************************************************
 This file is GENERATED by bin/gen_esb, do not edit it manually
*********************************************************************************************************************/

#ifndef AIR_CXX_COMPILER_GRAPH_EAGER_STYLE_GRAPH_BUILDER_OP_EmbeddingHashTableExport_CPP_H_
#define AIR_CXX_COMPILER_GRAPH_EAGER_STYLE_GRAPH_BUILDER_OP_EmbeddingHashTableExport_CPP_H_
#include <utility>
#include "es_tensor_holder.h"
#include "es_graph_builder.h"
#include "es_tensor_like.h"
#include "es_log.h"
#include <iostream>
#include "es_EmbeddingHashTableExport_c.h"
namespace ge {
namespace es {

struct EmbeddingHashTableExportOutput {
  std::vector<EsTensorHolder> keys;
  std::vector<EsTensorHolder> counters;
  std::vector<EsTensorHolder> filter_flags;
  std::vector<EsTensorHolder> values;
};
/**
 * @note at least one of the following input arguments should be EsTensorHolder object:
 *   table_handles
 *   table_sizes
 *   embedding_dims
 *   bucket_sizes
 * @note user needs to provide following inputs for dynamic output numbers:
 *   keys_num: dynamic output number of keys
 *   counters_num: dynamic output number of counters
 *   filter_flags_num: dynamic output number of filter_flags
 *   values_num: dynamic output number of values
 */
inline EmbeddingHashTableExportOutput EmbeddingHashTableExport(const EsTensorLike & table_handles, const EsTensorLike & table_sizes, const EsTensorLike & embedding_dims, const EsTensorLike & bucket_sizes, int64_t keys_num, int64_t counters_num, int64_t filter_flags_num, int64_t values_num, const char * export_mode="all", bool filtered_export_flag=false) {
  auto *owner_graph_builder = ResolveBuilder(table_handles, table_sizes, embedding_dims, bucket_sizes);
  ES_ASSERT_NOTNULL(owner_graph_builder, "Failed to resolve owner builder: please ensure at least one input tensor or an explicit owner_builder is provided when supported.");
  auto out = EsEmbeddingHashTableExport(table_handles.ToTensorHolder(owner_graph_builder).GetCTensorHolder(), table_sizes.ToTensorHolder(owner_graph_builder).GetCTensorHolder(), embedding_dims.ToTensorHolder(owner_graph_builder).GetCTensorHolder(), bucket_sizes.ToTensorHolder(owner_graph_builder).GetCTensorHolder(), keys_num, counters_num, filter_flags_num, values_num, export_mode, filtered_export_flag);
  std::vector<EsTensorHolder> keys_dynamic_outs;
  keys_dynamic_outs.reserve(out.keys_num);
  for (int64_t dyn_idx = 0; dyn_idx < out.keys_num; ++dyn_idx) {
    keys_dynamic_outs.emplace_back(out.keys[dyn_idx]);
  }
  std::vector<EsTensorHolder> counters_dynamic_outs;
  counters_dynamic_outs.reserve(out.counters_num);
  for (int64_t dyn_idx = 0; dyn_idx < out.counters_num; ++dyn_idx) {
    counters_dynamic_outs.emplace_back(out.counters[dyn_idx]);
  }
  std::vector<EsTensorHolder> filter_flags_dynamic_outs;
  filter_flags_dynamic_outs.reserve(out.filter_flags_num);
  for (int64_t dyn_idx = 0; dyn_idx < out.filter_flags_num; ++dyn_idx) {
    filter_flags_dynamic_outs.emplace_back(out.filter_flags[dyn_idx]);
  }
  std::vector<EsTensorHolder> values_dynamic_outs;
  values_dynamic_outs.reserve(out.values_num);
  for (int64_t dyn_idx = 0; dyn_idx < out.values_num; ++dyn_idx) {
    values_dynamic_outs.emplace_back(out.values[dyn_idx]);
  }
  return {keys_dynamic_outs, counters_dynamic_outs, filter_flags_dynamic_outs, values_dynamic_outs};
}
}  // namespace es
}  // namespace ge
#endif
